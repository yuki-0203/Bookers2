
<div class="alart">
      <% if @book.errors.any? %>
        <%= @book.errors.count %>error prohibited this obj from being saved:
        <ul>
        <% @book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      <% end %>
</div>
<div class="Userinfo">
  <h2>User info</h2>
    <div class="Userimag">
      <% if @user.profile_image.present? %>
        <%= attachment_image_tag @user, :profile_image, format: 'jpeg', size: '100x100' %>
      <% else %>
         <%= image_tag 'default.png', size: '100x100' %>
      <% end %>
    </div>
    
   <table class="table">
    <tbody>
      <tr></tr>
      <tr>
        <th>name</th>
        <th><%= @user.name %></th>
      </tr>
      <tr>
        <th>introduction</th>
        <th><%= @user.introduction%></th>
      </tr>
    </tbody>
  </table>
    <div class="row">
    <%if  @user == current_user %>
        <%= link_to edit_user_path(@user), class: "btn btn-outline-secondary btn-blok", method: :get do %>
           <i class="fas fa-user-cog"></i>
           <% end %>
       <% end %>
    </div>
  </div>

<div class="col-md-5 col-sm-10 rounded px-0 my-5">
<div class="New book">
  <h2 class="mt-3">New book</h2>
    <div class="alart">
    </div>
  </div>

  
    <div class="form-group">
      <%= form_with model: @book, local:true do |f| %>
        <h4>Title</h4>
          <%= f.text_field :title , class:"form-control book_title" %>
        <h4>opinion</h4>
          <%= f.text_area :body , class:"form-control book_body"%>
        <%= f.submit 'Create Book',class:"btn btn-success" %>
      <% end %>
    </div>
</div>
